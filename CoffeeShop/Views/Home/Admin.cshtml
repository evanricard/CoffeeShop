@{
    ViewBag.Title = "Admin";
}

@model CoffeeShop.Models.Item
<h1>Admin View</h1>

@*<form action="/Home/ListItems" method="get">
    <h3>Item</h3>
    <select name="Item">
        @foreach (var item in ViewBag.ItemList)
        {
            <option value="@item">@item</option>
        }
    </select>
    <input type="submit" value="Search by " />
</form>
<br />*@
<table class="table">
    <tr>
        <th>Name</th>
        <th>Description</th>
        <th>Quantity</th>
        <th>Price</th>
    </tr>
    @foreach (var item in ViewBag.ItemList)
    {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @item.Quantity
            </td>
            <td>
                @item.Price
            </td>
            @*<td><a href="/Home/DeleteCustomer?CustomerID=@CustomerRecord.CustomerID" onclick="return confirm('Are you sure?')">Delete</a></td>
            <td><a href="/Home/UpdateCustomer?CustomerID=@CustomerRecord.CustomerID">Update</a></td>*@
        </tr>
    }

</table>